services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: selah
      POSTGRES_PASSWORD: selah
      POSTGRES_DB: selah
    ports:
      - "5432:5432"
    volumes:
      - selah_db_data:/var/lib/postgresql/data

volumes:
  selah_db_data:
